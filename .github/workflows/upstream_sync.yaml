on: 
  workflow_dispatch:

name: Upstream Sync

jobs:
  run_sample_script:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Check-out latest code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ssh-key: ${{ secrets.GIT_PRIVATE_KEY }}
        env:
          GH_TOKEN: ${{ github.token }}
      - name: Run Bash script
        env:
          GH_TOKEN: ${{ github.token }}
        run: ./arista-ci-scripts/upstream_sync.sh
      - name: Send status email
        if: success() || failure()
        run: |
          mail_to="arajeev@arista.com"
          subject="Upstream Sync Status"
          body_text_file="arista-ci-scripts/upstream_sync_status_email_body.txt"
          attachment_path="upstream_sync_status.txt"
          ./arista-ci-scripts/mailer.py --mail_to="$mail_to" --subject="$subject" --body_text_file="$body_text_file" --attachment_path="$attachment_path" --sender_mail_id="${{ secrets.GMAIL_USER_NAME }}" --sender_mail_password="${{ secrets.GMAIL_PASSWORD }}"
        shell: bash
